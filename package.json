{
  "name": "bamazon",
  "version": "1.0.0",
  "description": "A Rather annoying Assignment.",
  "main": "BamazonCustomer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  
  },
  "dependencies": {
    "mysql": "^2.11.1",
    "prompt": "^1.0.0"
  }
}
